name: sukoon_properties
description: "Sukoon Properties Official Complete Native Mobile Application"
publish_to: 'none'
version: 1.0.0+100

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  webview_flutter: ^4.7.0
  url_launcher: ^6.2.5
  google_fonts: ^6.1.0
  http: ^1.2.0
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
